## Weather API
Create a Web API using ASP.NET Core with controllers.

A. Implement Swagger documentation in the project.

Fetch the /weatherforecast API and display the data using HTML, CSS, and JavaScript.

The frontend should not  be  the part of the Web API project; it can be a separate project.

Use appropriate icons and illustrations to make the interface visually appealing.